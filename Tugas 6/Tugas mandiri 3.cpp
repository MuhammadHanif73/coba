#include <stdio.h>

int main() {
	 
    // Mencetak deret angka 1 hingga 1024 dengan kelipatan 2
    int i, value = 1;
 
    for (i = 0; i <= 10; ++i) {
        printf(" %d\n", value);
        value *= 2;
    }

    return 0;
}
